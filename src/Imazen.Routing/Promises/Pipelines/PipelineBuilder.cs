namespace Imazen.Routing.Promises.Pipelines;

public class PipelineBuilder
{
    // var sourceCacheOptions = new ServerlessCacheEngineOptions
    // {
    //     SeriesOfCacheGroups = null,
    //     SaveToCaches = null,
    //     RequestRouter = routingEngine,
    //     BlobFactory = blobFactory,
    //     UploadQueue = null,
    //     Logger = logger
    // };
    // var imagingOptions = new ImagingMiddlewareOptions
    // {
    //     Logger = logger,
    //     BlobFactory = blobFactory,
    //     //TODO
    //     WatermarkingLogic = new WatermarkingLogicOptions(null, null)
    // };
    // var outputCacheOptions = sourceCacheOptions;
    //     
    // pipeline = new ServerlessCacheEngine(null, sourceCacheOptions);
    // pipeline = new ImagingMiddleware(pipeline,imagingOptions);
    // pipeline = new ServerlessCacheEngine(pipeline, outputCacheOptions);
}